#include <iostream>
using namespace std;

int factorial(int n) {
    if (n == 0 || n == 1) return 1;
    return n * factorial(n - 1);
}

int main() {
    int num;
    cout << "请输入一个整数: ";
    cin >> num;
    cout << "阶乘为: " << factorial(num) << endl;
    return 0;
}
